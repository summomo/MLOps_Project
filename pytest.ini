[pytest]
; NOTE: This warning is emitted by upstream MLflow at import time
; (Pydantic protected namespace "model_*"). Remove after upstream fix.
filterwarnings =
    ignore:Field "model_name" has conflict with protected namespace "model_".*:UserWarning